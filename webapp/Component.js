sap.ui.define(["sap/ui/core/UIComponent"], function (UIComponent) {
  "use strict";

  return UIComponent.extend("ui5.butterflies", {
    metadata: {
      manifest: "json",
    },
  });
});
